# IDE / dev environment — type checking, EDA, baseline only.
# Heavy QLoRA training runs on the DGX Spark (GB10) container, which provides
# GPU-compiled bitsandbytes / flash-attn that cannot be installed here.
#
#   pip install -r requirements.txt
#
# torch CPU-only for IDE support:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Core data / EDA
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
pyarrow>=15.0

# Rung-1 baseline
lightgbm>=4.3

# Transformer stack (training stack lives in the Spark container)
torch>=2.1
transformers>=4.44
peft>=0.12
trl>=0.9
datasets>=2.20
accelerate>=0.33
bitsandbytes>=0.43        # GPU only; no-op import on CPU dev box

# Utilities
tqdm>=4.66
PyYAML>=6.0

# Kaggle CLI / model hub
kaggle>=1.8.0
kagglehub>=0.4.1
